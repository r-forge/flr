# Set the body of the text
$BODYTEXT = "bgcolor=\"C6B5DE\" text=\"000000\"";

# This ensures that some figures do not end up with a grey background
$WHITE_BACKGROUND = 1;

# 1 =  use links to external postscript images rather than inlined bitmap
# images.
# $PS_IMAGES = 1;
$TRANSPARENT_FIGURES = 0;
$WHITE_BACKGROUND = 1;

# Generate the images in this location
$TMP = '/tmp';

# Tell LATEX:
$LATEX_COLOR = "\\pagecolor{white}";

$ADDRESS = "<A HREF=\"mailto:marco.kienzle\@gmail.com\">Marco Kienzle</A><BR><STRONG>
\n$address_data[1]</STRONG>";

sub bottom_navigation_panel { 
"$PgSQLdoc <HR> <BR>\n" }

# Do not reuse old images to create the HTML file
$REUSE = 0;

1;
